# 🚀 GitHub Setup - Schritt für Schritt

## 1. Git Repository initialisieren

```bash
# Im Projekt-Ordner
git init
git add .
git commit -m "Initial commit: Terminbuchungs-App v1.2"
```

## 2. GitHub Repository erstellen

### Option A: Via GitHub Website
1. Gehe zu https://github.com/new
2. Repository Name: z.B. `terminbuchung-app` oder `event-booking-app`
3. **Private** oder Public wählen
4. ⚠️ **NICHT** "Initialize with README" anklicken (haben wir schon!)
5. **Create repository** klicken

### Option B: Via GitHub CLI (falls installiert)
```bash
gh repo create terminbuchung-app --private --source=. --remote=origin
```

## 3. Remote Repository verbinden

Nach Erstellung bekommst du eine URL wie:
```
https://github.com/DEIN-USERNAME/terminbuchung-app.git
```

Dann:
```bash
git remote add origin https://github.com/DEIN-USERNAME/terminbuchung-app.git
git branch -M main
git push -u origin main
```

## 4. Wichtige Hinweise ⚠️

### ✅ Was wird gepusht:
- Source Code (src/)
- Dokumentation (docs/)
- Konfiguration (astro.config.mjs, etc.)
- Dependencies (package.json)
- Public Assets

### ❌ Was wird NICHT gepusht:
- `.env` (Secrets!)
- `node_modules/` (zu groß)
- `dist/` (Build-Ausgabe)
- `.wrangler/` (lokale Dev-Files)

→ Alles durch `.gitignore` geschützt ✅

## 5. Environment Variables sichern

⚠️ **WICHTIG**: `.env` wird nicht gepusht!

Erstelle separate Dokumentation:

```bash
# Erstelle Beispiel-Datei für andere Entwickler
cp .env .env.example

# Entferne echte Werte
nano .env.example
# Ersetze echte Werte mit:
GOOGLE_CLIENT_ID=your-client-id-here
GOOGLE_CLIENT_SECRET=your-secret-here
# etc.

# Commit .env.example
git add .env.example
git commit -m "Add environment variables example"
git push
```

## 6. GitHub Secrets einrichten (optional)

Falls du GitHub Actions nutzen möchtest:

1. GitHub Repository → **Settings**
2. **Secrets and variables** → **Actions**
3. **New repository secret** für jede Variable:
   - `GOOGLE_CLIENT_ID`
   - `GOOGLE_CLIENT_SECRET`
   - `ADMIN_PASSWORD`
   - etc.

## 7. Collaboration Setup (optional)

Falls andere Entwickler mitarbeiten:

```bash
# Settings → Collaborators → Add people
```

Dann können sie:
```bash
git clone https://github.com/DEIN-USERNAME/terminbuchung-app.git
cd terminbuchung-app
npm install
# .env erstellen mit ihren eigenen Credentials
npm run dev
```

## 8. Branching Strategy

### Empfohlene Struktur:
```
main          → Production-ready Code
development   → Development Branch
feature/*     → Feature Branches
hotfix/*      → Dringende Fixes
```

### Workflow:
```bash
# Neues Feature
git checkout -b feature/neue-funktion
# ... Änderungen ...
git commit -m "Feature: Neue Funktion hinzugefügt"
git push origin feature/neue-funktion
# → Pull Request auf GitHub erstellen

# Nach Merge → lokal aktualisieren
git checkout main
git pull origin main
```

## 9. Repository Protection (empfohlen)

GitHub Settings → Branches → **Add rule** für `main`:
- ✅ Require pull request reviews before merging
- ✅ Require status checks to pass before merging
- ✅ Require branches to be up to date before merging

## 10. README Anpassen

Passe die GitHub URL im README an:

```bash
# In README.md
https://github.com/DEIN-USERNAME/DEIN-REPO-NAME.git
```

Ersetze `DEIN-USERNAME` und `DEIN-REPO-NAME` mit echten Werten!

---

## ✅ Fertig!

Deine App ist jetzt auf GitHub! 🎉

**Nächste Schritte:**
- [ ] Repository URL im README anpassen
- [ ] `.env.example` erstellen
- [ ] GitHub Secrets einrichten (falls CI/CD)
- [ ] Collaborators hinzufügen (falls Team)
- [ ] Branch Protection aktivieren

